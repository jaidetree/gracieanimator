# Core
Django==5.1.4
psycopg[binary]==3.2.3
dj-database-url==2.3.0
django-environ==0.11.2
whitenoise==6.8.2
gunicorn==23.0.0

# Media / image renditions
Pillow==11.3.0
django-imagekit==6.1.0

# Admin drag-and-drop reordering for models with an order field
django-admin-sortable2==2.3.1

# Media storage: Cloudflare R2 (S3-compatible) via django-storages + boto3
django-storages==1.14.4
boto3==1.35.99

# Test harness
pytest==8.3.4
pytest-django==4.9.0
factory-boy==3.3.1

# End-to-end browser tests (opt-in: `pytest -m e2e`, needs `playwright install chromium`)
pytest-playwright==0.8.0
playwright==1.60.0

# Lint + format (config in ruff.toml)
ruff==0.15.20
