# Environment — .envrc is committed; personal overrides + legacy dotenv are not
.envrc.local
.env

# Python
__pycache__/
*.py[cod]
.venv/
.direnv/

# Django
/staticfiles/
# static/css/stylesheet.css is committed (built by `make build`/`make dev`),
# so prod's git-push deploy + collectstatic ships current styles. See README.

# Local Postgres cluster
/.pg/

# Test / tooling caches
.pytest_cache/
.coverage
htmlcov/
